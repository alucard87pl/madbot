# Copy to .env and fill in after creating your Discord application.
# Never commit .env or any file containing real tokens.

# Discord bot token (Developer Portal → Your App → Bot → Reset Token / Copy)
DISCORD_TOKEN=

# Application ID (Developer Portal → Your App → General Information → Application ID)
DISCORD_APPLICATION_ID=

# Optional: default wiki for /wiki when no per-guild override is set
# WIKI_PROVIDER=memory-alpha
